# 이음(E-UM) Backend — FastAPI

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9   # /assets/ocr 멀티파트 업로드
email-validator>=2.1      # Pydantic EmailStr

# --- 4단계(실제 구현) ---
httpx>=0.27               # Supabase PostgREST / Gemini REST 호출
PyJWT>=2.8                # Supabase JWT(HS256) 로컬 검증
cryptography>=42          # 디지털 유산 AES(Fernet) 암호화

# Supabase 는 PostgREST/GoTrue REST 를 httpx 로 직접 호출하므로 supabase-py 불필요.
# Gemini 도 REST 호출이라 google-generativeai SDK 불필요.
